# quant-stratum
**A production-ready 2×3 Hierarchical Hidden Markov Model for market regime detection using NIFTY 50 and India VIX data.**

[![Python 3.13](https://img.shields.io/badge/python-3.13-blue.svg)](https://www.python.org/downloads/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Status](https://img.shields.io/badge/Status-Production%20Ready-brightgreen.svg)]()
[![Project](https://img.shields.io/badge/Project-Complete-success.svg)]()

> **Project Status: ✅ COMPLETE** (Week 6 - February 11, 2026)  
> Production-ready system with full automation, web dashboard, and comprehensive documentation.

## Overview

This project implements a sophisticated regime detection system for Indian equity markets using a **2×3 Hierarchical HMM**:

- **SLOW Layer**: Durable vs Fragile regimes (long-term structural market conditions)
- **FAST Layer**: Calm vs Choppy vs Stress regimes (short-term market dynamics)

The system processes 15 years of NIFTY 50 and India VIX data through a deterministic feature engineering pipeline, generating 37 normalized features for regime classification.

## Architecture

```
Data Ingestion → Feature Engineering → HMM Training → Regime Prediction
     ↓                    ↓                  ↓               ↓
  NIFTY 50         SLOW Features      Slow HMM       Durable/Fragile
  India VIX        FAST Features      Fast HMM       Calm/Choppy/Stress
                   Normalization      Cascade         → 6 States
```

### Hierarchical Regime Structure

```
├─ DURABLE (Low structural risk)
│  ├─ Calm: Low vol, positive returns
│  ├─ Choppy: Moderate vol, mixed returns
│  └─ Stress: Short-term shocks in stable regime
│
└─ FRAGILE (High structural risk)
   ├─ Calm: Deceptive stability before crisis
   ├─ Choppy: Erratic behavior, regime uncertainty
   └─ Stress: Full crisis mode
```

## Features

### Data Pipeline
- ✅ **15 years** of historical data (2010-2026): 3,681 trading days
- ✅ **Data quality checks**: Outlier detection, missing data handling
- ✅ **NSE trading calendar**: Dynamic API-based holiday fetching (2011+) with 2010 fallback
- ✅ **Intelligent caching**: API responses cached locally to avoid redundant calls
- ✅ **Deterministic pipeline**: Reproducible feature generation

### Feature Engineering
- ✅ **37 features** engineered from OHLCV + VIX
  - 17 SLOW features (6M/12M windows)
  - 20 FAST features (1D-3M windows)
- ✅ **Rolling z-score normalization** (252D SLOW, 60D FAST)
- ✅ **Zero data leakage**: Validated with shift-by-1 test
- ✅ **Outlier clipping**: ±5σ for numerical stability

### Testing
- ✅ **Leakage tests**: Shift-by-1 validation
- ✅ **Rolling window integrity**: Correct window sizes
- ✅ **Determinism**: Reproducible results
- ✅ **Stationarity**: Z-score properties verified

## Installation

### Prerequisites
- Python 3.13+
- Node.js 18+ (for the frontend dashboard)
- Windows/Linux/macOS

### Setup

```bash
# Clone repository
git clone https://github.com/<your-username>/ML-Regime-Classifier.git
cd ML-Regime-Classifier

# Create virtual environment
python -m venv venv

# Activate (Windows PowerShell)
& "venv\Scripts\Activate.ps1"

# Or activate (Linux/macOS)
source venv/bin/activate

# Install Python dependencies
pip install -r requirements.txt

# (Optional) Install frontend dependencies
cd frontend && npm install && cd ..
```

## Repository Structure

After cloning, the repo contains **only source code**. You must generate data, features, and models before running the system.

```
ML-Regime-Classifier/
│
├── data/                              # Data pipeline
│   ├── ingestion/
│   │   ├── fetch_nse.py               # Download NIFTY/VIX from Yahoo Finance
│   │   └── nse_calendar.py            # NSE trading calendar (dynamic API)
│   ├── quality/
│   │   ├── outlier_detection.py       # Detect return outliers, volume spikes
│   │   └── missing_handler.py         # Handle missing data
│   └── processed/                     # ⬇ GENERATED (empty after clone)
│       └── market_data_historical.csv # Created by fetch_nse.py
│
├── market_regime/                     # Market regime classification system
│   ├── features/
│   │   ├── fast_features.py           # 20 FAST features (1D-3M windows)
│   │   ├── slow_features.py           # 17 SLOW features (6M/12M windows)
│   │   ├── normalization.py           # Rolling z-score normalization
│   │   ├── feature_store.py           # Feature pipeline orchestration
│   │   └── *.csv                      # ⬇ GENERATED by feature pipeline
│   ├── models/
│   │   └── hmm_regime_models.joblib   # ⬇ GENERATED by training pipeline
│   ├── output/
│   │   └── daily_inference_state.json # ⬇ GENERATED at runtime
│   └── scripts/
│       ├── build_features.py          # Feature engineering entry point
│       ├── daily_automation.py        # Full daily pipeline orchestrator
│       ├── daily_inference.py         # Daily regime prediction
│       ├── daily_inference_runtime.py # Inference runtime engine
│       ├── daily_ingestion.py         # EOD data fetching
│       ├── daily_ops.py               # Operations & health checks
│       ├── evaluate_model.py          # Model evaluation & reporting
│       ├── quarterly_retrain.py       # Quarterly retraining with validation
│       ├── run_retrain_if_triggered.py# Drift-triggered retraining
│       ├── startup_catchup.py         # Backfill missing dates on startup
│       ├── compute_adaptive_alpha.py  # Adaptive smoothing parameter
│       ├── walkforward_validation.py  # Walk-forward backtesting
│       ├── verify_csv_data.py         # Data integrity verification
│       └── test_*.py                  # Script-level tests
│
├── behavior_regime/                   # Behavior regime classification
│   ├── features/                      # ⬇ GENERATED
│   ├── models/                        # ⬇ GENERATED
│   └── scripts/
│       ├── behavior_pipeline.py       # Behavior regime pipeline
│       ├── behavior_regimes.py        # Regime definitions
│       ├── behavior_evaluation.py     # Evaluation metrics
│       └── force_retrain_and_verify.py# Force retrain utility
│
├── backend/                           # FastAPI backend
│   ├── main.py                        # API endpoints
│   ├── decision_engine.py             # Decision logic
│   └── requirements.txt               # Backend-specific deps
│
├── frontend/                          # React dashboard
│   ├── src/
│   │   ├── App.jsx                    # Main dashboard component
│   │   ├── main.jsx                   # React entry point
│   │   └── index.css                  # Styles (Tailwind)
│   ├── index.html                     # HTML entry point
│   ├── package.json                   # Node dependencies
│   ├── vite.config.js                 # Vite build config
│   ├── tailwind.config.js             # Tailwind config
│   └── postcss.config.js              # PostCSS config
│
├── tests/                             # Validation tests
│   ├── test_features.py               # Feature engineering tests
│   ├── test_val.py                    # Model validation (3M window)
│   ├── test_2y_val.py                 # Model validation (2Y window)
│   └── test_new_nifty500*.py          # Retrain evaluation tests
│
├── logs/                              # ⬇ GENERATED at runtime
│   ├── market_regime/                 # Training logs, drift baselines
│   └── behavior_regime/               # Behavior pipeline logs
│
├── output/                            # ⬇ GENERATED at runtime
│   ├── market_regime/                 # Predictions, reports, visualizations
│   └── behavior_regime/               # Behavior predictions
│
├── requirements.txt                   # Python dependencies
├── QUICKSTART.md                      # Quick start guide
├── WHAT_CHANGED.md                    # Changelog
└── README.md                          # This file
```

> **Legend**: Items marked ⬇ GENERATED are created by the pipeline at runtime. They are not included in the repository and must be generated locally.

### Directory Setup After Clone

The following directories will be created automatically when you run the pipeline. If you want to create them manually:

```bash
mkdir -p data/processed
mkdir -p market_regime/features
mkdir -p market_regime/models
mkdir -p market_regime/output
mkdir -p behavior_regime/features
mkdir -p behavior_regime/models
mkdir -p logs/market_regime
mkdir -p logs/behavior_regime
mkdir -p output/market_regime/daily_operations
mkdir -p output/market_regime/health_checks
mkdir -p output/market_regime/timeline_rows
mkdir -p output/market_regime/retrain_archives
mkdir -p output/behavior_regime
```

## Usage

### Step 1: Data Ingestion

Fetch 15+ years of NIFTY 50 and India VIX data from Yahoo Finance:

```bash
python data/ingestion/fetch_nse.py --years 15 --output data/processed/market_data_historical.csv
```

This creates `data/processed/market_data_historical.csv` (~3,700 rows of OHLCV + VIX data).

The NSE trading calendar automatically fetches holidays from the NSE API for years 2011+ and caches responses in `data/ingestion/.cache/`.

### Step 2: Data Quality Checks

```bash
python data/quality/outlier_detection.py
python data/quality/missing_handler.py
```

### Step 3: Feature Engineering

Build the complete feature matrix (37 features from OHLCV + VIX):

```bash
cd market_regime
python scripts/build_features.py
```

This creates:
- `market_regime/features/slow_features_matrix.csv` (17 SLOW features)
- `market_regime/features/fast_features_matrix.csv` (20 FAST features)
- `market_regime/features/final_features_matrix.csv` (37 normalized, ML-ready features)

### Step 4: Model Training

Train the 2×3 Hierarchical HMM:

```bash
python market_regime/scripts/quarterly_retrain.py
```

This creates `market_regime/models/hmm_regime_models.joblib`.

### Step 5: Run Daily Pipeline

```bash
python market_regime/scripts/daily_automation.py --build-features
```

### Step 6: Start the Dashboard

```bash
# Backend (FastAPI)
uvicorn backend.main:app --reload --port 8000

# Frontend (in a separate terminal)
cd frontend && npm run dev
```

- Backend API: http://localhost:8000/docs
- Dashboard: http://localhost:5173

### Step 7: Validation

```bash
python tests/test_features.py
```

## Feature Engineering

### 37 Features (17 SLOW + 20 FAST)

| Feature | Window | Formula | Regime Signal |
|---------|--------|---------|---------------|
| `rv_252_norm` | 12M | Rolling volatility (252D) | High = Fragile |
| `max_drawdown_126_norm` | 6M | Max peak-to-trough | Large = Fragile |
| `vix_shock_norm` | 3M | (VIX - MA) / σ | >2σ = Stress |
| `ret_1_norm` | 1D | Daily log return | Large neg = Stress |
| `skewness_60_norm` | 3M | Return asymmetry | Negative = Stress |

All features are normalized via rolling z-scores (252D for SLOW, 60D for FAST) and clipped at ±5σ.

## Key Capabilities

- **Daily Automation**: EOD data fetch → feature build → inference → health checks
- **Quarterly Retraining**: Automatic retrain with validation gates and rollback
- **Drift Detection**: BOCPD (Bayesian Online Changepoint Detection) + feature drift (KS test, PSI)
- **Emergency Retrain**: Triggered on drift/crash detection
- **Walk-Forward Validation**: Rolling 3-year train / 6-month test
- **Web Dashboard**: Real-time regime display with probability bars, timeline, and metrics

## Contributing

1. Fork the repository
2. Create a feature branch
3. Run `python tests/test_features.py` before submitting
4. Submit a pull request

## License

MIT License - See LICENSE file for details.

## References

### Academic
1. **Hidden Markov Models**: Rabiner (1989) - "A Tutorial on Hidden Markov Models"
2. **Regime Switching**: Hamilton (1989) - "A New Approach to the Economic Analysis of Nonstationary Time Series"
3. **Financial Volatility**: Andersen & Bollerslev (1998) - "Answering the Skeptics"

### Data Sources
- **NIFTY 50**: Yahoo Finance (`^NSEI`)
- **India VIX**: Yahoo Finance (`^INDIAVIX`)
- **NSE Holidays**: National Stock Exchange of India API

### Python Libraries
- **pandas** / **numpy**: Data manipulation
- **hmmlearn**: Hidden Markov Models
- **scikit-learn**: Preprocessing & metrics
- **yfinance**: Yahoo Finance API
- **FastAPI**: Backend API
- **React** + **Tailwind CSS**: Frontend dashboard
